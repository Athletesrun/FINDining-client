export type Restaurant = {
  name: string,
  url: string,
  img: string,
  rating: number,
  price: number,
  eta: number,
  category: string[],
  address: string[],
  location: number[],
  reviews: object[]
}
